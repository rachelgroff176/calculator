export function add(x, y = 0) {
  return x + y;
}

export function subtract(x, y = 0) {
  return (x - y);
}

export function multiply(x, y = 1) {
  return (x * y);
}

export function divide(x, y = 1) {
  return (x / y);
}